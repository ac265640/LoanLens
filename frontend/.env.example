# Copy to frontend/.env (gitignored). Points the dev server at the deployed
# API, so the dashboard runs against real data with no AWS access needed.
# The API allows any origin (CORS *), so http://localhost:5173 works.
VITE_API_URL=https://kwazm9gei8.execute-api.us-east-1.amazonaws.com/prod
