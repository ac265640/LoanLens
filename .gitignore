node_modules/
__pycache__/
*.pyc
.venv/
venv/
.env
.aws-sam/
.DS_Store
dist/
build/
frontend/node_modules/
frontend/.env
*.log

# Regenerable via training/generate_data.py + training/train_*.py —
# not committed to keep the repo lean; data/sample/ holds the small
# curated demo tape used by the ingestion demo, which IS committed.
data/raw/

# Local tooling config, not part of the project itself
.claude/

# Secrets and local environment. Never commit these. AWS credentials live in
# ~/.aws/credentials (`aws configure`) or AWS_* environment variables, not here.
.env
.env.*
!.env.example
*.pem
*.key
credentials.csv
*_accessKeys.csv
rootkey.csv
.aws/
*.tsbuildinfo
