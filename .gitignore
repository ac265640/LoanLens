# Dependencies and build output
node_modules/
frontend/node_modules/
.venv/
venv/
__pycache__/
*.pyc
.aws-sam/
dist/
build/
*.tsbuildinfo

# Secrets and local environment. Never commit these. AWS credentials live in
# ~/.aws/credentials (`aws configure`) or AWS_* environment variables, not here.
.env
.env.*
!.env.example
frontend/.env
*.pem
*.key
credentials.csv
*_accessKeys.csv
rootkey.csv
.aws/

# Logs, caches, editor and OS files
*.log
.pytest_cache/
.coverage
htmlcov/
.idea/
.vscode/
.DS_Store

# Regenerable via training/generate_data.py + training/train_*.py, so not
# committed. data/sample/ holds the small demo tape, which IS committed.
data/raw/

# Local tooling config, not part of the project
.claude/
